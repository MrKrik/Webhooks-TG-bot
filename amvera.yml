meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
run:
  persistenceMount: /data
  command: python init_db.py && python main.py && python3 -m uvicorn app:app --reload --host 0.0.0.0 --port 5000
  containerPort: 5000
